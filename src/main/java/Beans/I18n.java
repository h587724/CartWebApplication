package Beans;

import java.util.Locale;

public class I18n {
    Locale enLocale = new Locale.Builder().setLanguage("en").setRegion("GB").build();
    Locale noLocale = new Locale.Builder().setLanguage("no").setRegion("OSL").build();
    Locale esLocale = new Locale.Builder().setLanguage("es").setRegion("ES").build();
}
